{profiles, [
    {test, [{deps, [proper]}]}
]}.

{pre_hooks, [
    {compile, "zig build"}
]}.

{eunit_opts, [verbose, debug_info]}.
{deps, []}.

{relx, [
    {overlay, [
        {copy, "zig-out/lib/liberlang-python.so", "zig-out/lib/liberlang-python.so"}
    ]}
]}.
